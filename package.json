{
  "name": "get-release-branch-version",
  "version": "1.0.0",
  "type": "module",
  "description": "Gets the version number of a release branch",
  "main": "./dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/valadas/get-release-branch-version.git"
  },
  "keywords": [
    "github",
    "action",
    "get",
    "branch",
    "version"
  ],
  "author": "valadas",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/valadas/get-release-branch-version/issues"
  },
  "homepage": "https://github.com/valadas/get-release-branch-version#readme",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "@vercel/ncc": "^0.45.0",
    "typescript": "^5.9.3"
  }
}
